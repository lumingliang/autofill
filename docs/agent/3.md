写一个脚本。
1. 查询总结模板的public接口（需要找到接口路径）,然后得到模板名称和摘要以及模板内容。
2. 调用字段管理 里面的public接口， ，创建/更新字段。 传参
appkey = af_1fzDujUFl7SLg9L3CWMSV5upBT4GU1bR 页面名称是 用户信息页 字段组名称是default;字段名称是场景分类 
 根据模板名称和摘要，创建一个下拉类型的字段,摘要对应 填写指引，模板名称对应 字段标签和字段名（需要修改后端，字段不做校验）; 该接口需要具备在字段组不存在时，自动创建一个关联该appkey的字段组。字段不存在时，自动创建一个关联该appkey和字段组的字段,(如果字段已存在，则增加字段和该字段组的关联关系接口，通过字段名来判断)
 注意这些查询都需要根据appkey查询到对应tenant_id
3. 根据1中的结果。需要遍历每个模板名称 + 模板内容,模板内容里面有  ${customer_name} 之类的字段要解析出来，有多个这样的字段。然后遍历这些字段。 调用 调用字段管理 里面的public接口，创建/更新字段。 传参 
页面名称是 用户信息页 字段组名称是 服务记录-{模板名称}， 字段名是 提取出来的{customer_name}之类的。填写指引 传 请填写xx字段，没有则设置为空字符串
模板内容 需要保存到 字段组的output_templates字段



按功能拆分：把不同业务逻辑拆成单独 py 文件
按层级拆分：路由放 router、逻辑放 service、工具放 utils
大函数直接拆成多个小函数，再分到不同模块 禁止一个文件代码行数大于600，需要整体重构和拆分,整体重构后端代码的结构，该分层分层、该拆拆95

 