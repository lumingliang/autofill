 编写一个查询数据的agent助手的实施文档。输入(聊天记录 query 字段 +OpenAPI规范 doc字段)

[OpenAPI解析器] → 动态生成API调用工具
[意图提取与参数映射] → 将自然语言转换为API参数
    ↓
[Agent执行器] → 调用API获取结果
    ↓

使用langchain的OpenAPIToolkit动态生成API调用工具

   self.llm = ChatOpenAI(
            model=model,
            temperature=settings.TEMPERATURE,
            api_key=settings.OPENAI_API_KEY
        )
        
        self.max_iterations = max_iterations
        
        # 1. 加载OpenAPI规范并动态生成工具
        self.spec = OpenAPISpec.from_xxx
        self.toolkit = OpenAPIToolkit.from_llm_and_spec(self.llm, self.spec)
        self.tools = self.toolkit.get_tools()

提示词中让agent自己决策是否要调用工具。决策当前的查询结果是否满足用户需求。不满足需要继续调用工具查询。满足后需要返回查询结果。的前n条数据（同时openai的接口文档中已经说明了响应的格式，agent应该能解析响应）
    
    