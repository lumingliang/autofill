# 多租户功能测试用例

## 测试环境
- 前端: http://localhost:3100/
- 后端: http://localhost:9999/
- 测试账号: 
  - 超级管理员: admin / 123456
  - 单租户用户: user1 / 123456 (所属租户: 测试租户A)
  - 多租户用户: user2 / 123456 (所属租户: 测试租户A、测试租户B)
  - 浏览器测试用户: browseruser / 123456 (所属租户: 测试租户A)

## 已修复问题
1. ✅ 租户路由路径修复: `/api/v1/tenant/tenants/*` → `/api/v1/tenant/*`

---

## 一、登录功能测试

### TC001: 超级管理员登录
**测试结果**: ✅ 通过
**测试详情**:
```bash
curl -X POST http://localhost:9999/api/v1/base/access_token \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"123456"}'
```
**响应**:
```json
{
  "code": 200,
  "data": {
    "access_token": "...",
    "username": "admin",
    "tenants": [],
    "need_select_tenant": false,
    "current_tenant_id": null
  }
}
```

### TC002: 单租户用户登录
**测试结果**: ✅ 通过
**测试详情**:
```bash
curl -X POST http://localhost:9999/api/v1/base/access_token \
  -d '{"username":"user1","password":"123456"}'
```
**响应**:
```json
{
  "code": 200,
  "data": {
    "username": "user1",
    "tenants": [{"id":1,"name":"测试租户A","domain":"tenant-a"}],
    "need_select_tenant": false,
    "current_tenant_id": null
  }
}
```

### TC003: 多租户用户登录
**测试结果**: ✅ 通过
**测试详情**:
```bash
curl -X POST http://localhost:9999/api/v1/base/access_token \
  -d '{"username":"user2","password":"123456"}'
```
**响应**:
```json
{
  "code": 200,
  "data": {
    "username": "user2",
    "tenants": [
      {"id":1,"name":"测试租户A","domain":"tenant-a"},
      {"id":2,"name":"测试租户B","domain":"tenant-b"}
    ],
    "need_select_tenant": true,
    "current_tenant_id": null
  }
}
```
**说明**: 当 `need_select_tenant` 为 `true` 时，前端需要弹出租户选择弹窗。

---

## 二、租户管理功能测试

### TC004: 创建租户
**测试结果**: ✅ 通过
**测试详情**:
```bash
curl -X POST http://localhost:9999/api/v1/tenant/create \
  -H "token: $ADMIN_TOKEN" \
  -d '{"name":"测试租户A","domain":"tenant-a","description":"...","is_active":true}'
```
**响应**:
```json
{
  "code": 200,
  "msg": "创建成功",
  "data": {
    "tenant": {"id": 1, "name": "测试租户A", ...},
    "admin_role_id": 3,
    "message": "租户创建成功，已自动创建管理员角色：测试租户A管理员"
  }
}
```

### TC005: 查看租户列表
**测试结果**: ✅ 通过
```bash
curl "http://localhost:9999/api/v1/tenant/list?page=1&page_size=10" \
  -H "token: $ADMIN_TOKEN"
```

---

## 三、用户管理功能测试

### TC006: 创建用户并分配租户 (API测试)
**测试结果**: ✅ 通过
```bash
curl -X POST http://localhost:9999/api/v1/user/create \
  -H "token: $ADMIN_TOKEN" \
  -d '{
    "username":"user1",
    "email":"user1@test.com",
    "password":"123456",
    "tenant_ids":[1],
    "role_ids":[3]
  }'
```

### TC007: 浏览器创建用户并分配租户
**测试结果**: ✅ 通过
**测试步骤**:
1. 使用 admin/123456 登录系统
2. 进入"系统管理" → "用户管理"
3. 点击"新建用户"按钮
4. 填写用户信息:
   - 用户名称: browseruser
   - 邮箱: browseruser@test.com
   - 密码: 123456
   - 确认密码: 123456
5. 选择所属租户: 测试租户A
6. 选择角色: 测试租户A管理员
7. 点击保存

**预期结果**: ✅ 用户创建成功，列表中显示新用户
**实际结果**: ✅ 新增成功提示，用户列表中出现 browseruser 用户，显示所属租户为"测试租户A"

### TC008: 用户列表显示租户信息
**测试结果**: ✅ 通过
**验证点**:
- ✅ 用户列表显示"所属租户"列
- ✅ admin 用户显示为超级用户
- ✅ user1 显示所属租户: 测试租户A
- ✅ user2 显示所属租户: 测试租户A、测试租户B
- ✅ browseruser 显示所属租户: 测试租户A

### TC009: 租户筛选功能
**测试结果**: ✅ 通过
**验证点**:
- ✅ 用户管理页面显示租户筛选下拉框
- ✅ 可以选择特定租户筛选用户

---

## 四、角色管理功能测试

### TC010: 查看角色列表
**测试结果**: ✅ 通过
**测试步骤**:
1. 进入"系统管理" → "角色管理"
2. 查看角色列表

**实际结果**: 角色列表显示正常，包含以下角色：
- 管理员（系统角色）
- 普通用户（系统角色）
- 测试租户A管理员（所属租户：测试租户A）
- 测试租户B管理员（所属租户：测试租户B）

**验证点**:
- ✅ 角色列表显示"所属租户"列
- ✅ 系统角色显示为"系统角色"
- ✅ 租户管理员角色正确显示所属租户名称
- ✅ 租户筛选下拉框正常显示

---

## 五、多租户用户登录测试

### TC011: 多租户用户登录并选择租户
**测试结果**: ✅ 通过
**测试步骤**:
1. 退出当前账号
2. 使用 user2/123456 登录
3. 预期弹出租户选择弹窗
4. 选择一个租户进入系统

**实际结果**: 
- ✅ 登录成功
- ✅ 弹出"选择租户"弹窗，提示"您属于多个租户，请选择要登录的租户"
- ✅ 显示两个租户选项：测试租户A、测试租户B
- ✅ 选择租户后成功登录，显示当前租户"测试租户A"

### TC012: 单租户用户直接登录
**测试结果**: ✅ 通过
**测试步骤**:
1. 退出当前账号
2. 使用 user1/123456 登录（user1只属于一个租户）
3. 预期直接登录成功，不需要选择租户

**实际结果**: 
- ✅ 登录成功，直接跳转到工作台
- ✅ 没有弹出租户选择弹窗（符合预期，因为只有一个租户）

---

## 六、浏览器测试总结

### 已测试功能
| 功能 | 状态 | 备注 |
|------|------|------|
| 超级管理员登录 | ✅ 通过 | admin/123456 |
| 用户管理页面访问 | ✅ 通过 | 显示租户筛选和用户列表 |
| 创建用户并分配租户 | ✅ 通过 | 创建 browseruser 成功 |
| 用户列表显示租户信息 | ✅ 通过 | 正确显示所属租户 |
| 用户管理-租户筛选 | ✅ 通过 | 超管可见租户筛选，可筛选用户 |
| 角色管理-租户字段 | ✅ 通过 | 显示所属租户列和筛选功能 |
| 角色管理-租户筛选 | ✅ 通过 | 超管可见租户筛选，可筛选角色 |
| 多租户用户登录并选择租户 | ✅ 通过 | user2登录后弹出租户选择弹窗 |
| 单租户用户直接登录 | ✅ 通过 | user1直接登录，无需选择租户 |

### 待测试功能
- [ ] 租户切换功能
- [ ] 数据隔离验证

---

## 测试问题记录

| 问题ID | 问题描述 | 状态 | 修复方案 |
|--------|----------|------|----------|
| 1 | 租户API路径错误 | ✅ 已修复 | 移除 `/tenants` 前缀 |
| 2 | 角色管理页面Vue语法错误 | ✅ 已修复 | 删除多余的 `</content>` 和 `</invoke>` 标签 |
| 3 | 租户选择接口参数格式错误 | ✅ 已修复 | 修改后端接口接收JSON body而非query参数 |

---

## 测试截图

### 1. 登录页面
- 页面正常显示，包含用户名和密码输入框

### 2. 用户管理页面
- 显示用户列表，包含"所属租户"列
- 显示租户筛选下拉框
- 显示新建用户按钮

### 3. 新建用户弹窗
- 包含用户名称、邮箱、密码、确认密码输入框
- 包含所属租户选择（可多选）
- 包含角色选择（含租户管理员角色）
- 保存按钮正常工作
