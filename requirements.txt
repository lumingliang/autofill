# Web Framework
fastapi==0.115.12
uvicorn==0.34.0
uvloop==0.21.0; sys_platform != 'win32'

# Database (aiosqlite, asyncclick, iso8601, dictdiffer, pypika-tortoise 会自动安装)
tortoise-orm==0.23.0
aerich==0.8.1

# Pydantic & Settings (annotated-types, pydantic-core, typing-extensions, python-dotenv 会自动安装)
pydantic==2.10.5
pydantic-settings==2.7.1
email-validator==2.7.0

# Authentication & Security
pyjwt==2.10.1
passlib==1.7.4
argon2-cffi==23.1.0

# HTTP Client (anyio, certifi, httpcore, idna, h11, click, sniffio 会自动安装)
httpx==0.28.1

# Kafka
confluent-kafka==2.8.0

# Redis
redis>=5.0.0

# Image Processing
pillow==11.0.0

# Configuration
toml==0.10.2

# Logging
loguru==0.7.3

# Form/Multipart Support (FastAPI 文件上传需要)
python-multipart==0.0.20

# Development Tools (可选，生产环境可移除; mypy-extensions, packaging, pathspec, platformdirs 会自动安装)
black==24.10.0
isort==5.13.2
ruff==0.9.1
